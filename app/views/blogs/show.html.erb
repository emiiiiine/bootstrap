<h1>ブログ詳細画面</h1>
<p>タイトル: <%= @blog.title %></p>
<p>内容: <%= @blog.content %></p>
<p>投稿日時: <%= @blog.created_at %></p>
<p>投稿者: <%= @blog.user.name %></p>
<% unless @blog.user_id == current_user.id %>
  <p><%= link_to 'ダイレクトメッセージ', new_contact_path, class: 'btn btn-success' %></p>
  <% if @favorite.present? %>
    <%= link_to 'お気に入り解除する', favorite_path(id: @favorite.id), method: :delete, class: 'btn btn-danger' %>
  <% else %>
    <%= link_to 'お気に入りする', favorites_path(blog_id: @blog.id), method: :post, class: 'btn btn-primary' %>
  <% end %>
<% end %>
<%= link_to "ブログ一覧画面にもどる", blogs_path %> | 
<%= link_to "お気に入り一覧にもどる", user_path(current_user.id) %>